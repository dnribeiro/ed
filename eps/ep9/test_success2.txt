struct node {
    int data[MAX_SIZE];
    struct node* next;
};

void function() {
    for (int i = 0; i < n; i++) {
        if (array[i] > 0) {
            hash_table[key] = {value, index};
            printf("Processing item %d\n", i);
        }
    }
}